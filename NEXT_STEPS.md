# 次の作業ターゲットと今後の見通し

> 更新日: 2026-08-08（コンパクト本番完了・T1確定反映）
> 目的: コンテキスト切り替え後に作業を再開できるよう、現在地・次にやること・見通しを1枚にまとめたもの。
> 新しいセッションでは、まず本ファイルと [CLAUDE.md](.claude/CLAUDE.md) を読み、続きを行う。

---

## 0. 次セッションの最初の1手（最重要）

**→ T1（FINALIZE_GUIDANCE 実装）を実行する。** これはユーザー確定の次のターゲット
（「L1 で C4 が B0 に負ける構造的原因の改善が next step」— 2026-08-08 ユーザー指示）。
詳細は §2 T1。実装 → `pytest` → C4 単独検証（dev, 1試行）の順で進める。

---

## 1. 現在地（2026-08-08 時点）

**Step 2（測定系の較正）は完了。コンパクト本番（8試行）も完了し、C4 のみ 0/2 で品質未達が明確になった。次の作業は「L1 で C4 が B0 に負ける構造的原因」の改善（T1 確定済み）。**

### 完了済み

| 項目 | 状態 | 内容 |
|---|---|---|
| 閾値較正 | ✅ | 棄権 unknown_level≥0.85 / confidence<0.15（旧 0.7/0.3 は全試行 abstain の過敏さ） |
| 探索ループ | ✅ | 高重要度 unknown を構造分析で解決、unknown_level を継続低下。成果は最終生成に反映 |
| ルーブリック改訂 | ✅ | Risk 軸を「リスク認識の適切さ」へ再定義、Quality/Value に誠実さ基準を追加 |
| B1（Reflexion） | ✅ | `poc/conditions.py::run_b1`。振り返り1回＋再生成 |
| B2（Self-Refine） | ✅ | `poc/conditions.py::run_b2`。自己批評1回＋改善生成、評価は独立評価器 |
| 空応答再試行 | ✅ | `adapters/claude_client.py::_call`。dev検証で error=null を確認 |
| harness 例外捕捉 | ✅ | `_run_one` が例外を decision=error に変換、実験全体が落ちない |
| 停止条件（設計） | ✅ | 十分性 / 収束（Δ<0.01×2） / 上限（5回） / 保留 の4条件。**収束判定のみ未実装** |
| 思考品質レベル | ✅ | L1 エコノミー(1回) / L2 スタンダード(5回) / L3 ディープ(8回)。**開発既定 = L1**（`config/params.yaml`） |
| report 単一条件対応 | ✅ | `poc/report.py` 効果量のガード。C4 単独でもクラッシュしない |
| テスト | ✅ | **77件全通過**（`pytest tests/ -v`） |
| コンセプト再考 | ✅ | 「考え方を自ら改善し、必要な水準に達したら素早く止まるAI」へ。README・設計書に反映 |
| **コンパクト本番** | ✅ | `run-27e35794` 完了（8試行）。パイプライン動作は全4条件で確認。**ただし C4 は 0/2 で品質未達** |

### コンパクト本番の結果（run-27e35794、完了）

```
B0  2/2 = 100%  (0.763, 0.738)
B1  2/2 = 100%  (0.732, 0.715)
B2  2/2 = 100%  (0.730, 0.728)
C4  0/2 = 0%    (0.663 abstain / 0.680 explore)  ← 唯一の未達
```

- ⚠️ この実行は **8 試行**（`--per-condition` デフォルト=2 のため 4条件×2リピート）。4試行にしたい場合は `--per-condition=1` を明示
- error=null（空応答修正は全試行で有効）。C4 は探索しても 0.70 未達（abstain せず探索を実行した2回目も 0.680）
- n=2/条件のため統計判定は不可（参考: 効果量 −100pp・判定=reject だが意味なし）

### 現時点の正直な評価（2026-08-08 追記）

> **「素のAI（B0）より優れているか」→ 現時点では NO。実証されていない。**
>
> - 唯一の統計的比較（Step 1、N=20）は効果量 **−50.0pp** で仮説H棄却。Step 2 は「測定を公平にする」作業であり、**C4 の質そのものを上げたわけではない**
> - コンパクト本番でも C4 は 0.66〜0.68 で、B0/B1/B2（0.72〜0.76）に全試行で劣る。**「L1 でも素のAIよりかなりマシ」は設計目標であって、未達成**
> - **構造的原因が特定済み**: 探索で知識を得たのに最終生成が「まだ無知」前提（EXPLORE/ABSTAIN_GUIDANCE）で書かせる設計欠陥。詳細は §2 T1

---

## 2. 次の作業ターゲット（優先度順）

### T1（最優先）: L1 で C4 が B0 に負ける構造的原因の改善

**原因（実装精読で特定済み）**: [conditions.py:742-752](poc/conditions.py#L742-L752) で、探索ループ後に unknown_level が 0.25 以下まで下がらない限り（= ほぼ常に）最終生成に `EXPLORE_GUIDANCE`（「まだ十分な情報を持っていません。自信のない点を推測で埋めず」）または `ABSTAIN_GUIDANCE`（「無理に断定せず」）が付く。
→ **探索で known・仮説・部分解決を得たのに、「無知・慎重モード」で書かされ、Quality（明確・一貫）と Value（影響力）が下がる**。B0 は留保なしに直接書くので 0.76 を出せる。

**改善案（FINALIZE_GUIDANCE）**:
1. 新しい最終生成ガイダンス `FINALIZE_GUIDANCE` を追加:
   「探索で得た知識（known・仮説・部分解決）に基づき、確定的な分析を示せ。確実な点は具体的・断定的に。残る不確実性は『前提』として明示せよ。」
2. 最終生成の指針を「知識が増えたか」で選ぶ:
   - 探索で known が増えた／仮説が得られた → `FINALIZE_GUIDANCE`（確定的に書く）
   - 真の abstain（知識が増えなかった）→ 現行の控えめな指示を維持
3. 効果: L1 でも1回の探索で得た知識を最終成果物に活かせる → 「素のAIよりマシ」を実現できる構造になる

**実装範囲**: `poc/conditions.py`（ガイダンス選択部 + 定数追加）+ テスト追加。小さい変更。
**検証手順**: `pytest tests/ -v` → `python main.py --condition=C4 --tasks=1 --taskset=dev --per-condition=1` で C4 単独の改善を確認 → 期待: overall 0.70 以上 or 0.66/0.68 より改善

### T2: 収束判定の実装（停止条件の完成）

- 現状: 停止条件4つのうち「収束（unknown_level 改善幅 Δ<0.01 が連続2回）」だけが未実装
- 場所: `poc/conditions.py::run_c4` の探索ループに追加
- 意義: 探索が効かなくなったら（改善が頭打ちになったら）すぐ止める = 「必要な水準に達したら素早く止まる」の要
- 設計書: `docs/03_コアコンポーネント/00_数値定義書.md` §3.4 追記を「実装済み」に更新

### T3: L3 ディープの実装と検証

- 現状: params.yaml に定義済み（8回）だが未実装（config 的には動く）
- 意義: C4 の品質 0.70 未達（0.66〜0.68）に対し、L3 で到達できるかの検証材料
- 手順: T1 の改善後、L1 → L2（5回）→ L3（8回）と増やして C4 の overall 推移を見る
- 開発方針（ユーザー指示）: **開発段階は基本 L1（エコノミー）で試す**

### T4: 結果に基づく設計書の追記

- コンパクト本番の実測（成功率・error 有無・所要時間）を 11/09 §13 に追記
- 停止条件・思考品質レベル・T1 の構造的原因の実測知見を 03/00 §3.4 に追記

### T5（将来・ユーザー判断）: 正式な N=20 本番

- 統計的な効果量判定には `--all --tasks=20 --taskset=prod`（80試行、約3〜4時間）が必要
- 判定帯域・追試ルールは `poc/preregistration-step2.md` §5・§6 に従う
- **T1 で C4 が改善したことを確認してから実施**（現状のまま回しても棄却が続くだけ）

---

## 3. 今後の見通し

### 短期（次セッション1〜2回）

- **T1: 構造的原因の改善（FINALIZE_GUIDANCE）** — 探索で得た知識を最終生成に確定的に活かす。L1 で C4 が B0 よりマシになる構造を作る
- T1 の検証（C4 単独で 0.70 到達 or 0.66/0.68 より改善）
- 収束判定（T2）を入れて「素早く止まる」設計を完成させる
- 思考品質の調節（L1/L2/L3）を実際に使い分ける運用を固める

### 中期

- C1〜C3 成分別要因実験（Step 2 当初予定だったが後続タスクに延期済み）
  - 各機能（閾値較正 / 探索ループ / ルーブリック改訂）の寄与を分解して計測
- 人間較正サンプル50件（03/00 §2.6.1、ρ≥0.70）— ルーブリック改訂後の評価妥当性検証。**現在 ✗ 未実施**
- 可視化の実装（07/01 §16.5 構想）— 思考の軌跡（探索ループ・unknown 減少）を人間に見せる

### 長期

- 正式な N=20 本番で効果量判定（C4 vs B0/B1/B2）
- 思考品質レベルを実運用でどう使うか（エンドユーザー向け L1、重要タスク向け L3 など）の設計
- 停止条件・品質調節の知見を「知恵の評議会」本体（小説評価）へ応用

---

## 4. 環境メモ（再開時の注意）

- **実行環境**: DeepSeek ゲートウェイ（`https://api.deepseek.com/anthropic`）経由
  - 生成 = claude-sonnet-4-5 → **deepseek-v4-pro** / 評価 = claude-haiku-4-5 → **deepseek-v4-flash**
  - 認証は shell 環境変数 `ANTHROPIC_AUTH_TOKEN` + `ANTHROPIC_BASE_URL`（**APIキーを出力に含めない**）
- **実行コマンド**（`--per-condition=1` を明示すると 1条件×1回 になる）:
  - モック検証: `python main.py --all --tasks=5 --mock`
  - C4 単独検証（T1 後の改善確認）: `python main.py --condition=C4 --tasks=1 --taskset=dev --per-condition=1`
  - コンパクト本番: `python main.py --all --tasks=1 --taskset=prod --per-condition=1`（4試行）
  - 正式本番（ユーザー判断後）: `python main.py --all --tasks=20 --taskset=prod --per-condition=1`
- **Git**: コミットは1判断1コミット。**push はユーザー自身が行う**（勝手に push しない）

---

## 5. 参照

- 実装計画の正式版: `docs/11_実装計画とロードマップ/09_最初の動く試作品設計書.md`（§13 時間削減 / §15 Step 2 再定義）
- 数値定義の正版: `docs/03_コアコンポーネント/00_数値定義書.md`（§2.5 ルーブリック / §3.4 停止条件 / §3.5 棄権機構）
- 新規事前登録: `poc/preregistration-step2.md`
- メモリ: `~/.claude/projects/-Users-user-AI----------------/memory/`（insight-synapse-poc.md, insight-synapse-exploration.md）
